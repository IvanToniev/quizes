h1 Quizzes

table.table
  thead
    tr
      th Quiz
      th
  tbody
    - @quizzes.each do |quiz|
      tr
        td = quiz.title
        td = link_to 'Start Quiz', quiz_welcome_user_path(quiz)

br
